# the user to enter their name and stores it in the variable "name"
name = input("What is your name? ")

# the user to enter how they are feeling and stores it in the variable "response"
response = input("How are you? ")

# Prints a greeting message by concatenating the strings "Hello ", the value of the variable "name",
# ", ", and the value of the variable "response"
print("Hello " + name + ", " + response)
